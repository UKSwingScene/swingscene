name: Scrape and Deploy

on:
  schedule:
    - cron: '0 2 * * *'   # runs every day at 2am UTC
  workflow_dispatch:        # allows manual trigger from GitHub Actions tab

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install playwright
          playwright install chromium
          playwright install-deps chromium

      - name: Run scraper
        run: python scraper.py

      - name: Build HTML page
        run: python build.py

      - name: Commit and push updated page
        run: |
          git config user.name "SwingScene Bot"
          git config user.email "bot@ukswingscene.github.io"
          git add index.html events.json
          git diff --staged --quiet || git commit -m "Auto-update events $(date +%Y-%m-%d)"
          git push
